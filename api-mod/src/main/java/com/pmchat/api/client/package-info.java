/**
 * The PocketChat client API, for Fabric mods that want to read from or drive the
 * in-game messenger.
 *
 * <p>Start at {@link com.pmchat.api.client.PocketChatClient}. The package deliberately
 * has no Minecraft or Fabric types in it, so it compiles against any Minecraft version
 * and never breaks on a mappings change.
 *
 * @since 1.0.0
 */
package com.pmchat.api.client;
