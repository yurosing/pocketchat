/**
 * The raw {@code pmchat:media} wire protocol.
 *
 * <p>Only needed if you are speaking the protocol yourself — a proxy, a bridge, a client
 * for another platform. Ordinary server-side integration should use
 * {@link com.pmchat.api.PocketChatApi} and {@link com.pmchat.api.event} instead.
 *
 * @since 1.0.0
 */
package com.pmchat.api.protocol;
